spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

app:
  jwt:
    secret: test-secret-key-for-tests-only-qqqqqqqqqqqqqqqq
    access-token-ttl: 36000000000
    refresh-token-ttl: 864000000000

logging:
  level:
    org.testcontainers: DEBUG
    com.github.dockerjava: DEBUG